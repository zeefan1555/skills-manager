export declare const DEFAULT_PAGE_NUMBER = 1;
export declare const DEFAULT_PAGE_SIZE = 20;
export declare const REPO_PATH_HINT = "Defaults to repoPath parsed from git remote origin via getRepoName.";
